---
description: DApp/UI 样式用 tv() variant 与复合组件，禁止导出 Tailwind class 常量
globs: "src/**/*.{ts,tsx}"
alwaysApply: false
---

# Variant 优先 · 禁止 class 常量

> 设计系统细则：[`docs/foundation/runbook.md`](../../docs/foundation/runbook.md) · [`ui-tokens.md`](../../docs/foundation/ui-tokens.md)

## MUST NOT

- 新建或导出 Tailwind **class 常量**：`*Class`、`*ClassName`、裸字符串、`cn('…')` 模块级样式块
- 用平行 `*-layout.ts` 堆间距 / chrome 字符串
- 用 `group-data-[tab=*]/host` 在共享组件里堆 tab 响应式 — 样式归 tab 复合组件 + variant

## MUST

- 可复用视觉 → 组件内 **`tv()`**（与 `Button` / `Card` / `Text` 同文件或 tab 复合组件旁）
- 语义组合 → **复合组件 + variant prop**（如 `<Card surface="elevated">`、`<Section />`）
- 单用处间距 / 微调 → **JSX `className` inline**
- 多处相同结构 → **抽组件**，不要抽 class 字符串

## 示例

```tsx
// ❌ 禁止
export const swapMetricCardClass = cn('gap-1.5 rounded-md shadow-card …')
export const dappDetailSectionGapClass = 'mt-8.5 max-dapp:mt-6'

// ✅ 组件内 tv + variant prop
const swapMetricCard = tv({
  base: 'gap-1.5 rounded-md px-4 py-3.5 shadow-card …',
  variants: {
    sessionReady: { true: '[&_small]:hidden', false: '' },
  },
})

export function SwapMetricCard({ label, value }: Props) {
  const { sessionReady } = useDappHost()
  return (
    <MetricCard className={swapMetricCard({ sessionReady })} label={label} value={value} />
  )
}

// ✅ 多处块间距 → 组件（见 `shared/components/section.tsx`）
<Section>
  <Section.Title>{title}</Section.Title>
  {children}
</Section>
```
