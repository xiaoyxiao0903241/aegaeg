# 01 — 核：softPre ↔ approve 同配

**What to build:** 编排核在「需要授权」时，必须同时声明哪些预检原因可以先授权再继续；漏配时在编译期或既有核测中暴露，避免新路径「额度不足却永不授权」或「点了授权却仍被硬挡」。已有正确调用方行为不变（expand 优先：旧写法仍能过渡，或一次性改完核测覆盖的全部既有 call site）。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 有授权步骤时，可软过的预检原因与授权同配（类型或等价 API），漏配无法静默通过
- [x] 既有编排核单测仍绿：顺序、预检硬失败跳过授权、软预检走授权、实时硬挡在授权之后
- [x] 已使用编排核的路径（资产 / 质押等）在本票后仍可编译并保持原行为；若改签名，本票内改完这些 call site 或提供兼容过渡
- [x] 注释说明「为何授权后必须再读」用通俗中文，无黑话
