# 08 — 已合规路径行为测补缝

**What to build:** 为已经使用编排核的资产 / 质押等路径补上「软预检 → 授权 → 实时硬挡 → 不写」与「双读通过 → 写一次」行为测，防止全仓迁移期间误改已正确路径。

**Blocked by:** None — can start immediately（可与 02–03 并行）。

**Status:** done

- [x] 至少覆盖 Mixed 领取、赎回或 xmine、质押开仓或债券 zap 中的代表性路径（抽样即可，勿为测而测全矩阵）
- [x] 测的是外部行为（调用次数与阻断），不是源码字符串匹配冒充契约
- [x] 本票不改变产品行为；红测只暴露既有缺口或迁移回归
