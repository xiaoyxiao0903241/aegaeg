# 02 — Lucky 混合领取迁入编排核

**What to build:** 幸运奖混合领取提交改为走统一「预检 → 按需授权（若有）→ 实时复核 → 写」；仍只钉展示层选出的轮次，对该轮双读。intent 轮与 live 轮不一致、或 live 金额导致贡献闸变化时必须阻断，不得假放行。

**Blocked by:** 01 — 核 softPre ↔ approve 同配

**Status:** done

- [x] Lucky 提交经编排核；不再维护平行的手写双段序
- [x] 行为测：intent/live 轮或金额错配 → 阻断且不写
- [x] 行为测：双读通过 → 写入恰好一次且使用 live 快照
- [x] 用户可见领取成功/失败语义与迁前一致（不改按钮文案）
