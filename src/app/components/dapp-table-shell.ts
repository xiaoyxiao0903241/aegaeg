import { cn } from '~/lib/utils'

/** Standalone table / empty-state card — matches Figma elevated surface (PC + H5). */
export const dappTableCardShellClass = 'rounded-2xl bg-card shadow-card'

/** Table / empty area inside an outer metric card (Genesis contributions on H5). */
export const dappTableNestedShellClass =
  'max-dapp:border-0 max-dapp:bg-transparent max-dapp:px-0 max-dapp:py-0 max-dapp:shadow-none max-dapp:rounded-none'
